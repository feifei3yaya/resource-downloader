# Windows PowerShell 中文乱码问题解决方案

## 问题描述

在 Windows 系统上使用 PowerShell 进行文件操作时，中文文件名和控制台输出经常会出现乱码问题。

## 解决方案

### 1. 快速修复（当前会话）

在 PowerShell 脚本开头添加以下代码：

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$env:LANG = "zh_CN.UTF-8"
$env:LC_ALL = "zh_CN.UTF-8"
$PSDefaultParameterValues['*:Encoding'] = 'utf8'
```

### 2. 永久配置

运行 `setup_encoding.ps1` 脚本来永久配置：

```powershell
powershell -ExecutionPolicy Bypass -File setup_encoding.ps1
```

### 3. 手动设置 PowerShell 配置文件

编辑 PowerShell 配置文件（`$PROFILE`），添加以下内容：

```powershell
# PowerShell 编码配置
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$env:LANG = "zh_CN.UTF-8"
$env:LC_ALL = "zh_CN.UTF-8"
$env:PYTHONIOENCODING = "utf-8"
$PSDefaultParameterValues['*:Encoding'] = 'utf8'
```

### 4. 文件保存编码

- 使用 UTF-8 编码保存所有 PowerShell 脚本
- 在 VS Code 或其他编辑器中选择 "UTF-8 with BOM" 或 "UTF-8" 编码

## 已完成的工作

1. ✅ 成功抠图肥鸭战队logo，生成透明背景PNG
2. ✅ 修复文件乱码问题，正确重命名为 `肥鸭战队logo_透明背景.png`
3. ✅ 创建编码修复脚本 `setup_encoding.ps1`
4. ✅ 创建改进的抠图脚本 `process_logo.ps1`

## 结果文件

- 原始图片：`肥鸭战队logo.jpg`
- 抠图结果：`肥鸭战队logo_透明背景.png`
- 处理脚本：`process_logo.ps1`
- 编码配置：`setup_encoding.ps1`